# Requirements
- Git
- Docker
- Docker-compose
- Makefile support (Unix-like supported by default, NMAKE for Windows)

# Prerequisites
- Ensure :80 port is available

# Installation
- `make composed`
- `make db`
- Open http://localhost